version: "3.1"
services:
  client:
    restart: always
    stdin_open: true
    build:
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - /app/node_modules
      - ./client:/app
